defaults:
 - base

init_from: resume
train_time: ~

run_dir: ${data.data_type}_${data.sequence_type}_${data.target_type}_observation_size_${data.observation_size}_ground_truth_${data.ground_truth_length}/${experiment}/train_run_${model.model_type}_${general.train_time}/hydra_resume/
